# Bloquea el acceso web directo a esta carpeta.
# Apache 2.4+ (XAMPP moderno):
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
# Apache 2.2 (compatibilidad):
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
